import { Template, html } from "mvp";

export class  extends Template {
  constructor() {
    super(
      "{TemplateTemplate}",
      html("")
        .setClass("")
        .setId("id")
        .setHTML()
    );
  }
}
